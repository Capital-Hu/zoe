你是记忆压缩助手。请基于以下对话历史进行分层记忆压缩。

输出要求：
1. concise_summary：100字以内中文摘要，突出近期任务、患者状态和待办。
2. session_facts：当前会话内后续仍可能复用的稳定事实，输出为JSON数组字符串（例如：["事实1","事实2"]）。

约束：
- 只保留对后续对话有价值的信息。
- 不要编造未出现过的信息。
- 如果缺少可提炼事实，session_facts 输出 []。

对话历史：
{{history}}
